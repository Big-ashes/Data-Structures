#include <stdio.h>

//求解一个数字n的十进制表示的数字位数

int main(){
    int n;
    while(scanf("%d",&n) != EOF)
        printf("  has %d digits\n ",  printf("%d",n));
    return 0;
}

//printf函数返回值其实是一个int 包含输入的参数有多少位